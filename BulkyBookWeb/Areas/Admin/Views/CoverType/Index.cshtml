@using Microsoft.AspNetCore.Mvc.TagHelpers
@model IEnumerable<BulkyBook.Models.CoverType>

@{
    ViewData["Title"] = "Categories";
}
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">COVER TYPE LIST</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="CoverType" asp-action="Create" class="btn">
                <i class="bi bi-plus-lg"></i>
            </a>
        </div>
    </div>
</div>

<table class="table table-bordered table-striped" style="width: 100%">
    <thead>
    <tr>
        <th>
            Category Name
        </th>
    </tr>
    </thead>
    <tbody>
    @foreach (var obj in Model)
    {
        <tr>
            <td width="50%">
                @obj.Name
            </td>
            <td>
                <div class="w-75 btn-group" role="group">
                    <a asp-controller="CoverType" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-primary mx-2 ">
                        <i class="bi bi-pencil"></i>EDIT
                    </a>
                    <a asp-controller="CoverType" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-primary mx-2 ">
                        <i class="bi bi-trash"></i>DELETE
                    </a>
                </div>
            </td>
        </tr>
    }
    </tbody>
</table>